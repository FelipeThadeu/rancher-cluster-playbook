- name: Atualizar cache do zypper
  zypper:
    update_cache: yes

- name: Instalar pacotes básicos
  zypper:
    name:
      - vim
      - git
      - curl
      - wget
      - htop
      - tar
      - gzip
    state: present